#include"pch.h"
#include<string.h>
#include<math.h>
#include"functions.h"
#include<stdlib.h>
#define ISCHAR 0
#define ISNUM 1
int isinarray(char ch, char* str) {

}
int calc(char* str) {
	int strlength = strlen(str);
	char* savechar;
	int* savenum;
	BOOL* ctrl = (BOOL *)malloc(strlength - 1);
	for (int i=0;; i++) { 
		
	}
	return 0;
}